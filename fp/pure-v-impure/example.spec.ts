describe('example', () => {
    describe('collate', () => {
        it('should collate names and addresses', () => {

        });

        it('should split the name into first and last', () => {

        });
    })
})