let y: number = 0;
const f = (x: number): number => Date.now() % 2 ? x : (y = y + x + Date.now() % 2)


const a = () => {
    const x = 2;
}

































console.log(  f(x) === f(x)   );








